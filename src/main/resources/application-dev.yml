spring:
  datasource:
    url: jdbc:mysql:://localhost:3306/pizzeria?createDatabaseIfNoExist=true
    username: usrpizzeria
    password: user.pizzeria.db
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        "[format_sql]": true
  sql:
    init:
      mode: always
server:
  port: 8090